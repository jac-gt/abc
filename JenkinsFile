pipeline {
  agent any
  
  stages {
    stage("build"){
      steps {
        echo 'Hello World from feature/US123'
        echo 'Branch is ${params.GIT_BRANCH} and source id is ${params.SOURCE_ID}'
      }
    }
  }

}
