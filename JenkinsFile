pipeline {
  agent any
  
  parameters { string(name: 'GIT_BRANCH', defaultValue: 'master', description: 'branch to build') }
  
  stages {
    stage("build"){
      steps {
        echo 'Hello World from feature/US123'
        echo $GIT_BRANCH
      }
    }
  }

}
